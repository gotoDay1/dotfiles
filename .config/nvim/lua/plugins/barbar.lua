return {
	{
		"romgrk/barbar.nvim",
		lazy = false,
		opts = {
			-- animation = true,
			insert_at_start = true,
		},
		version = "^1.0.0", -- optional: only update when a new 1.x version is released
	},
}
